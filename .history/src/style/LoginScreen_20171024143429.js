import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    // flex: 1,
    backgroundColor: '#fff',
    // alignItems: 'center',
    justifyContent: 'center'
  },
  logo: {
    marginTop: 50,
    marginBottom: 50,
    alignItems: 'center',
    justifyContent: 'center'
  }
})